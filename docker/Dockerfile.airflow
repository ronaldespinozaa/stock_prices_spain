FROM apache/airflow:2.7.3

USER root

# Instalar dependencias adicionales
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# Volver al usuario airflow para la ejecución
USER airflow